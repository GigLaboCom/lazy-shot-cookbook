{
  "name": "lazy-shot-scenario-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Deliberately broken demo app used to shoot the cookbook's assets",
  "scripts": {
    "dev": "vite --port 5173 --strictPort"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
